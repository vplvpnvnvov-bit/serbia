# Локальные правила проекта Serbia

## Коммиты и деплой
- НЕ коммитить и НЕ деплоить изменения без явной команды пользователя
- Можно вносить любые правки в файлы, проверять синтаксис (`node --check`), но git commit + npm run deploy — только по команде «коммить», «задеплой» или «пушить»
