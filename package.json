{
  "name": "belgrad-relocation-app",
  "version": "6.33.19",
  "private": true,
  "description": "Serbia Relocation Tracker",
  "scripts": {
    "build": "node build.js",
    "deploy": "npm run build && surge --project . --domain serbia.surge.sh"
  }
}